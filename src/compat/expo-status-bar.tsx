/** Native status bar tint. On the web the <meta name="theme-color"> tag does it. */
export function StatusBar(_props: { style?: 'light' | 'dark' | 'auto'; hidden?: boolean }) {
  return null;
}
export default StatusBar;
